/**
 * 全站「诚实边界」文案的唯一可信源（对应 docs/ROADMAP.md §6，必须保留）。
 *
 * 页面与 API 的所有免责 / 风险边界说明统一从这里取，避免口径在各处手写时漂移。
 * 纯字符串、无 React 依赖，服务端（API note）与客户端（页面）皆可引用。
 */

/** 通用尾注：所有 AI / 量化产物统一以此收尾。 */
export const NFA = "仅供研究，不构成投资建议（NFA）。";

/**
 * 套利雷达 / 协整校准 / 配对回测：统计信号边界（§6 三条核心口径）。
 * 相对强弱择时 · 含市场 β（非市场中性）· 非无风险对冲套利 · 样本内会破裂 · 历史不代表未来 · 单边自担风险。
 */
export const ARB_BOUNDARY =
  "诚实边界：套利雷达与校准产物均为统计信号，是「相对强弱择时」、含市场 β（非市场中性）、非无风险对冲套利；A 股主板无融券，单边持有自担方向与 β 风险。协整为样本内性质、会破裂，历史回归率/胜率/收益不代表未来。" +
  NFA;

/** 回测 / 情景模拟边界。 */
export const BACKTEST_BOUNDARY =
  "历史回测 / 情景模拟不代表未来收益，样本内表现通常偏高、需看样本外。" + NFA;

/** 基本面质量分边界（单只自评）。 */
export const FUNDAMENTALS_BOUNDARY =
  "基本面质量分为透明加权的单只自评，不做同业对标、不预测未来；财务指标季度更新、估值/股息为实时推导。" +
  NFA;

/** AI 生成内容边界。 */
export const AI_BOUNDARY = "以上由 AI 依据公开行情与量化方法生成，可能有误，" + NFA;
